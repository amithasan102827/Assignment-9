import React from 'react';
import './NotFound.css';
import background from "../../images/404-error.png";

const NotFound = () => {
    return (
        <div className="not-found" style={{ backgroundImage: `url(${background})` }}>
      
      </div>
    );
};

export default NotFound;